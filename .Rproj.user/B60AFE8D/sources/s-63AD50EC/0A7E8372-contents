library(testthat)
library(pkghw4g6)

test_check("pkghw4g6")

# Examples

library(testthat)
test_that("check function with right input and wrong input"{
  estimate_area(B = 4367, seed = 10)
  estimate_area(B=bca,seed=csv)
})
